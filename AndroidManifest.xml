<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.seoulaccord.beacon">

    <application
        android:name=".BeaconConnection"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateAlwaysHidden">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".ReserveInfoActivity" />
        <activity android:name=".ReserveRoomActivity" />
        <activity
            android:name=".SmartKeyActivity"
            android:label="@string/title_activity_smart_key"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>